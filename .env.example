# Netlify / Astro environment variables
# Copy to .env.local for local development. On Netlify, set in Site settings > Environment variables.

# Required for sitemap and RSS (replace with your production URL)
# SITE_URL=https://your-site.netlify.app

# Optional: Analytics, form handling, etc.
# NETLIFY_FORM_SUBMIT=1
