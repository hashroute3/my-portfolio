Store publication PDFs in this directory.

Examples:
- /papers/query-resource-prediction.pdf
- /papers/placeholder-journal-article.pdf

Update the matching `pdfUrl` field in `src/data/publications.ts` after adding a file.
